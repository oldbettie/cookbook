<%= search_form_for(@q, url: search_path, method: :get) do |f| %>
    <%# these params allow different search functionality based on database tables or columns%>
    <%= f.search_field :name_or_instructions_or_user_username_or_region_or_category_i_cont_any, placeholder: "Search...", class: 'search-bar' %>
    <%= f.submit "Search!", class: "btn" %>
<% end %>
